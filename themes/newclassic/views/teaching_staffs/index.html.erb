    <div id="wrap">

  <%= render 'shared/side_menu' %>
               
            <div id="content">
                <!-- .outer -->
                <div class="container-fluid outer"  >
                            <div class="row-fluid">
                                                        <div class="span12 inner">
                            <!--Begin Datatables-->
                            <div class="row-fluid">
                                <div class="span12">
                                    <div class="box">
                                        <header>
                                            <div class="icons"><i class="icon-move"></i></div>
                                            <h5>Manage Teaching Staff Details </h5><%= link_to "Add New Teacher", "teaching_staffs/new", :class=> "btn btn-default btn-large", :style=> "margin:10px;float:right;" %>
                                        </header>
                                        <div id="collapse4" class="body">
                                            <table id="dataTable" class="table table-bordered table-condensed table-hover table-striped">
                                                <thead>
                                                  
     <tr>
        <th ></th>
       <th >Email</th>
       <th >Name</th>
       <th >Qualification</th>
       <th >Description</th>
       
       <th >Created At</th>

        <th style="text-align:center;"><%=t '.actions', :default => t("helpers.actions") %></th>
     </tr></thead>
<% @teachingstaff.each do |teachingstaff| %>
<tr><td>
<% if teachingstaff.user.image_blob != nil %>
<%= image_tag url_for(:controller=>"registrations",:action => 'user_image', :id => teachingstaff.user.id), :size => "30x30" %>
<% end  %>
 </td>
<td><%= teachingstaff.user.email %></td><td>
  <%= teachingstaff.name %></td><td>
<%= teachingstaff.qualification %></td><td>

<%= teachingstaff.description %></td><td>
<%=l teachingstaff.created_at, :format=>'%d %b. %Y %I:%M' %></td>


  <td> <%= link_to(edit_teaching_staff_path(teachingstaff)) do%><i class="icon icon-edit"></i><% end%>

    </td> 
    <td>
    <%= link_to(teaching_staff_path(teachingstaff),
                                   :method => :delete,
                      :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) ) do %>
           <i class="icon icon-trash"></i>
          <% end %>
        </td>

</tr>

<% end %>
</table>                                   </div>
                                    </div>
                                </div>
                            </div> 
                                
                            
                        
                    </div>
                    <!-- /.row-fluid -->
                </div>
                <!-- /.outer -->
            </div>
           </div>
            <!-- END CONTENT -->
<div class="left" style="padding: 0px;width: 210px;float: right;">
    <a href="accounts/new" class="btn btn-default btn-large" style="margin:10px;"><i class="icon icon-plus-sign"></i>Add New Account</a>
    <a href="courses/new" class="btn btn-default btn-large" style="margin:10px;"><i class="icon icon-book"></i>Add New Course</a>
    </div>


            <!-- #push do not remove -->
            <div id="push"></div>
            <!-- /#push -->
        
        <!-- END WRAP -->

        <div class="clearfix"></div>
       
