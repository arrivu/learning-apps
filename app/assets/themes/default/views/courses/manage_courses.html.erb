<!-- 

<div style="float:left">
  
</div>


<br>
<div class="row">
  <div class="span12 offset3" style="min-height:530px;">

<%- model_class = Course -%>

<table class="table table-condensed table-hover table table-striped">
  <thead>
    <tr ><td colspan="7"><h3 align = "center">Manage Course Library</h3></td></tr>
    <tr class="tab_head">
      <th></th>
      <th>Category</th>
      <th><%= model_class.human_attribute_name(:title) %></th>
      <th><%= model_class.human_attribute_name(:start_date) %></th>
      <th><%= model_class.human_attribute_name(:end_date) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>

    <% @courses.each do |course| %>
    <tr>

      <td>
        <%= if course.course_image !=nil
          image_tag url_for(:controller=>"courses", :action => 'show_image', :id => course.id), :size => "40x40" 
        end 
        %></td>
        <td><%= course.topic.name %></td>
        <td><%= link_to course.title, course_path(course) %></td>
        <td><%= course.start_date %></td>
        <td><%= course.end_date %></td>
        <td><%=l course.created_at, :format=>'%d %b. %Y %I:%M' %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
          edit_course_path(course), :class => 'btn btn-mini' %>
          <%=hidden_field t('.destroy', :default => t("helpers.links.destroy")),
          course_path(course),
          :method => :delete,
          :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
          :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
      <% end %>
   


  </tbody>

</table>


</div>

</div>

<div class='apple_pagination' align="center"><%= will_paginate @courses, :container => true  %> </div>


 -->
  <div id="wrap">

  <%= render 'shared/side_menu' %>
              
            <div id="content" class="span9">
               
                <div class="container-fluid outer"  >
                            <div class="row-fluid">
                                                        <div class="span12 inner">
                           
                            <div class="row-fluid">
                                <div class="span12">
                                    <div class="box">
     <header>
 <div class="icons"><i class="icon-move"></i></div>
   <h5>Manage Course Library </h5> <%= link_to "Add Course", "courses/new", :class=> "btn btn-default btn-large", :style=> "margin:10px;float:right;" %>
       </header>
                                                                            <div id="collapse4" class="body">
                                           
      <table id="dataTable" class="table table-bordered table-condensed table-hover table-striped">
        <thead>
       <tr class="tab_head">
      <th></th>
      <th>Category</th>
      <th><%= model_class.human_attribute_name(:title) %></th>
      <th><%= model_class.human_attribute_name(:start_date) %></th>
      <th><%= model_class.human_attribute_name(:end_date) %></th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.action', :default => t("helpers.action") %></th>
    </tr>
  </thead>
  <tbody>

    <% @courses.each do |course| %>
    <tr>

      <td>
        <%= if course.course_image !=nil
          image_tag url_for(:controller=>"courses", :action => 'show_image', :id => course.id), :size => "40x40" 
        end 
        %></td>
        <td><%= course.topic.name %></td>
        <td><%= link_to course.title, course_path(course) %></td>
        <td><%= course.start_date %></td>
        <td><%= course.end_date %></td>
        <td><%=l course.created_at, :format=>'%d %b. %Y %I:%M' %></td>
        <td>
          <%= link_to(edit_course_path(course)) do%><i class="icon icon-edit"></i><% end%>
        
        
          <%=hidden_field t('.destroy', :default => t("helpers.links.destroy")),
          course_path(course),
          :method => :delete,
          :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
          :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
      <% end %>
   


  </tbody>

</table>


                     
</div>
                                    </div>
                                </div>
                            </div> 

                    </div>
                </div>
            </div>
           </div>
            <!-- END CONTENT -->
 <div class="span2 left" style="padding: 0px;width: 210px;float: right;">
    <a href="accounts/new" class="btn btn-default btn-large" style="margin:10px;"><i class="icon icon-plus-sign"></i>Add Account</a>
    <a href="courses/new" class="btn btn-default btn-large" style="margin:10px;"><i class="icon icon-book"></i>Add Course</a>
    </div>


            <!-- #push do not remove -->
            <div id="push"></div>
            <!-- /#push -->
        
        <!-- END WRAP -->

        <div class="clearfix"></div>