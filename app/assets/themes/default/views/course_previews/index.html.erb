

 <div id="wrap">

  <%= render 'shared/side_menu' %>
               
            <div id="content">
                <!-- .outer -->
                <div class="container-fluid outer" style="background:#fff;">
                            <div class="row-fluid">
                                                        <div class="span12 inner">
                            <!--Begin Datatables-->
                            <div class="row-fluid">
                                <div class="span12">
                                    <div class="box">
                                       <header>
 <div class="icons"><i class="icon-move"></i></div>
   <h5>Manage Preview </h5> <%= link_to "Add Preview", "course_previews/new", :class=> "btn btn-default btn-large", :style=> "margin:10px;float:right;" %>
       </header>                          
                                        <div id="collapse4" class="body">
                                            <table id="dataTable" class="table table-bordered table-condensed table-hover table-striped">
                                              <thead>
          <tr class="tab_head">
            <th>Course </th>
            <th>Title</th>
            <th>Description</th>
            <th>Display Order</th>
            <th>Created At</th>

<th style="text-align:center;"><%=t '.actions', :default => t("helpers.actions") %></th>          </tr>
        </thead>
        <tbody>
          <% @previews.each do |preview| %>
          <tr>
            <td><%= preview.course.title %></td>
            <td><%= preview.name %></td>
            <td><%= preview.desc %></td>
            <td><%= preview.sequence %></td>
            <td><%=l preview.created_at, :format=>'%d %b. %Y %I:%M' %></td>

<td> <%= link_to(edit_course_preview_path(preview)) do%><i class="icon icon-edit"></i><% end%>

    </td> 
    <td>
    <%= link_to(course_preview_path(preview),
                                   :method => :delete,
                      :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) ) do %>
           <i class="icon icon-trash"></i>
          <% end %>
        </td>
          </tr>
          <% end %>
        </tbody>
      </table>

                               </div>
                            </div> 
                                  </div>
                            </div> 
                            
                        
                    </div>
                    <!-- /.row-fluid -->
                </div>
                <!-- /.outer -->
            </div>
              </div>
            <!-- END CONTENT -->
   <div class="left" style="padding: 0px;width: 210px;float: right;">
    <a href="accounts/new" class="btn btn-default btn-large" style="margin:10px;"><i class="icon icon-plus-sign"></i>Add New Account</a>
    <a href="courses/new" class="btn btn-default btn-large" style="margin:10px;"><i class="icon icon-book"></i>Add New Course</a>
    </div>


            <!-- #push do not remove -->
            <div id="push"></div>
            <!-- /#push -->
        
        <!-- END WRAP -->

        <div class="clearfix"></div>