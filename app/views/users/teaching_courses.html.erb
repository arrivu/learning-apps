<div id="wrap">
<%= render 'shared/side_menu' %>              
  <div id="content">             
    <div class="container-fluid outer"  >
     <div class="row-fluid">
       <div class="span12 inner">
                           
                            <div class="row-fluid">
                                <div class="span12">
                                    <div class="box">

   <div class="icons"><span class="badge badge-info">Users</span></div>
   <table >
   <tr>
   <td><%= form_tag "/teaching_courses", :method => 'get' do %></td>
   
   <td><%= label_tag(:search,"Course:") %></td>


  <td><%= select_tag :search, options_from_collection_for_select((current_user.teaching_staff.courses.map { |teaching_staff_course| teaching_staff_course.course }), "id", "title",:selected => params[:search]), {:prompt => 'ALL'} %></td> 

 
   <td valign="top"><%= submit_tag "Search", :name => nil,class:"btn btn-large btn-primary"  %></td>
   </tr>
 </table>
   
   <% end %>
  <div id="collapse4" class="body">
  
       <table id="dataTable" class="table table-bordered table-condensed table-hover table-striped">
                                                <thead>
      <tr class="tab_head">
        <th >Username</th>
        <th >Email</th>
        <th >Course</th>
        <th>Date</th>
      </tr>
    </thead>
    <tbody>
      
      <% @users.each do |user| %>
      <tr>
        <td ><%= link_to user.student.user.name %></td>
        <td><%= user.student.user.email %></td>
        <td><%= user.course.title %></td>
        <td><%=l user.created_at , :format=>'%d %b. %Y %I:%M'%></td>
        
      </tr>
      <% end %>

    </tbody>
   
  </table>
</div>


                                    </div>
                                </div>
                            </div> 

                    </div>
                </div>
            </div>
          </div>
            <!-- END CONTENT -->





            <!-- #push do not remove -->
            <div id="push"></div>
            <!-- /#push -->
        
        <!-- END WRAP -->

        <div class="clearfix"></div>
        <script>
$(document).ready(function() 
    { 
        $("#myTable").tablesorter(); 
    } 
); 
</script>  