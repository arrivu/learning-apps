<div class="container" style = "margin-top: 50px;">
<div class="span12">
<div class="span4">       
<div class='span5' style="font-size: 1.6em; font-family: Arial;  text-align:justify;">
  <b style="color: #b26b3a;"><%= @course.title%> </b><br/>
   </div>
  <div class='span5' style="font-size: 17px; font-family: verdana;  text-align: justify;">
      <%= @course.desc%>
</div>   

 <p><%= render 'reviewform' %></p>
 
</div>

 <div class="span6 right">
     
    <div class=""><%= simple_form_for(@comments,:url => url_for(:controller => 'courses',:action => 'review')) do |f| %>
    <% @comments.each do |comment| %>

    <div class="span6" style="margin-bottom:20px;float:left;margin-left: -20px;">
     <div class="reviewer1" style = "margin-left: -69px;
margin-bottom: -60px;">

      <% if comment.user.image_blob == nil %>
        <% if comment.user.omni_image_url == nil %>
        <%= gravatar_for_with_size comment.user, "50x50" %>               
        <% else  %>
        <%= image_tag(comment.user.omni_image_url,size: "50x50", alt: "", :class=>"img-circle")%> 
        <% end  %>
      <% else  %>
         <%= image_tag url_for(:controller => 'registrations',:action => 'user_image', :class=>"img-circle", :id => comment.user.id), :size => "50x50" %>
         <% end  %>
    </div>

    <h4><%= comment.user.name  %></h4>
    
    <div style = "color: rgb(128, 123, 123);"> reviewed-<%= distance_of_time_in_words(comment.created_at,Time.now)%> ago </div>

  <ul>

<li class="review-stars" >
  <a>
     <div style="height:40px;">

 <%= rating_for_user @course,comment.user, "rate"  %>

          </a>
</li>
</ul>


          <div class="bubble-content1" style = "margin-top: -13px;">
            <div class="point"></div> 

          <%= comment.comment  %></p>
          
          </div>
         

       
          <div class="clearfix"></div>
           <% if current_user.has_role? :admin or current_user.has_role? :account_admin %>
           <%= link_to(comment_path(comment), :style => "float: right; margin-right: 75px;",  :method => :delete,:confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) )  do  %>
           <i class="icon icon-trash"></i>
          <% end %>
      <% end %>   
    
        </div>
     

      
        
        
       

      </li>


 
       <%end%> 
<% end %>
</div>

</div>
</div>

    </div> 
 
