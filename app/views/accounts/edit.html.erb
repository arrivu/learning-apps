 <div id="wrap">
  <%= render 'shared/side_menu' %>

      <div id="content" class="span9">
                <!-- .outer -->
                <div class="container-fluid outer"  >
                            <div class="row-fluid">
                                                        <div class="span12 inner">
                            <!--Begin Datatables-->
                            <div class="row-fluid">
                                <div class="span12">
                                    <div class="box">
                        <div id="collapse4" class="body">
                                            <div class="icons"><span class="badge badge-info">  Edit Account Settings </span></div>
                                                 <table id="dataTable" >    <%= form_for (@account) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
<tr><td><%= f.label :name %></td>
<td><%= f.text_field :name %></td></tr>
<tr><td><%= f.label :organization %></td>
<td>
<%= f.text_field :organization %></td></tr>

<tr><td><%= f.label :no_of_users %></td>
<td>
<%= f.text_field :no_of_users %></td></tr>
<tr><td><%= f.label :no_of_courses %></td>
<td>
<%= f.text_field :no_of_courses %></td></tr>
<tr><td><%= f.label :support_script %></td>
<td>
<%= f.text_area :support_script, :style=>"visibility: visible;" %></td></tr>
<tr><td><%= f.label :google_analytics_script %></td>
<td>
<%= f.text_area :google_analytics_script, :style=>"visibility: visible;" %></td></tr>
<tr><td><%= f.label :activate %></td>
<td>
<%= f.check_box :active %></td></tr>
<tr>  <td><%= f.submit "Update" ,:class=>"btn btn-primary"%> </td>
   <td><%= link_to "Cancel", :back ,:class=>"btn btn-default"%> </td></tr>
<% end %></table>

                                   </div>
                                    </div>
                                </div>
                            </div> 
                                
                            
                        
                    </div>
                    <!-- /.row-fluid -->
                </div>
                <!-- /.outer -->
            </div></div>
            <!-- END CONTENT -->
 


            <!-- #push do not remove -->
            <div id="push"></div>
            <!-- /#push -->
        
        <!-- END WRAP -->

        <div class="clearfix"></div>






