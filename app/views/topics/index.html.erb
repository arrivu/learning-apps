<!-- <div style="float:left">

</div>

<br>
<div class="row">
  <div class="span12 offset3" style="min-height:530px;">
   
<%- model_class = Topic -%>
<table class="table table-condensed table-hover table table-striped">
  <thead>
    <tr><td colspan="4"><h3 align = "center">Manage Category</h3></td></tr>
    <tr class="tab_head">
      <th><%= model_class.human_attribute_name(:name) %></th>
      <th>Color</th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @topics.each do |topic| %>
      <tr>
        <td><%= link_to topic.name, topic_path(topic) %></td>
        <td><span style="width:20px;background-color:<%= topic.color %>">&nbsp;&nbsp;&nbsp;</span></td>
        <td><%=l topic.created_at, :format=>'%d %b. %Y %I:%M'  %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_topic_path(topic), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      topic_path(topic),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
</div>
</div>
<div class='apple_pagination' align="center"><%= will_paginate  @topics, :container => false %></div>
 -->

  <div id="wrap">

  <%= render 'shared/side_menu' %>
              
            <div id="content">
               
                <div class="container-fluid outer" style="background:#fff;">
                            <div class="row-fluid">
                                                        <div class="span12 inner">
                           
                            <div class="row-fluid">
                                <div class="span12">
                                    <div class="box">
   <header>
 <div class="icons"><i class="icon-move"></i></div>
   <h5>Manage Category </h5> <%= link_to "Add Category", "topics/new", :class=> "btn btn-default btn-large", :style=> "margin:10px;float:right;" %>
       </header>
 <div id="collapse4" class="body">
  <table id="dataTable" class="table table-bordered table-condensed table-hover table-striped">
      <thead>
        <tr class="tab_head">
      <th><%= model_class.human_attribute_name(:name) %></th>
      <th>Color</th>
      <th><%= model_class.human_attribute_name(:created_at) %></th>
      <th><%=t '.actions', :default => t("helpers.actions") %></th>
    </tr>
  </thead>
  <tbody>
    <% @topics.each do |topic| %>
      <tr>
        <td><%= link_to topic.name, topic_path(topic) %></td>
        <td><span style="width:20px;background-color:<%= topic.color %>">&nbsp;&nbsp;&nbsp;</span></td>
        <td><%=l topic.created_at, :format=>'%d %b. %Y %I:%M'  %></td>
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_topic_path(topic), :class => 'btn btn-mini' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      topic_path(topic),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-mini btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>                     
</div>
</div>
</div>
</div> 
</div>
 </div>
            </div>
           </div>
            <!-- END CONTENT -->
   <div class="left" style="padding: 20px;">
         <%= link_to "Add New Account", url_for(:controller => "accounts", :action => "new"), :class=> "btn btn-default btn-large", :style=> "margin:10px;" %>
           <%= link_to "Add New Course", url_for(:controller => "courses", :action => "new"), :class=> "btn btn-default btn-large", :style=> "margin:10px;" %>
            </div>


            <!-- #push do not remove -->
            <div id="push"></div>
            <!-- /#push -->
        
        <!-- END WRAP -->

        <div class="clearfix"></div>